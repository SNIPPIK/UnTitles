{
  "name": "watklok",
  "version": "0.5.0",
  "homepage": "https://github.com/SNIPPIK/UnTitles",
  "bugs": "https://github.com/SNIPPIK/UnTitles/issues",
  "type": "module",
  "author": {
    "name": "SNIPPIK",
    "url": "https://github.com/SNIPPIK"
  },
  "license": "BSD-3-Clause + custom restriction",
  "preferGlobal": true,
  "engineStrict": true,
  "types": "src/index.ts",
  "engines": {
    "node": ">=25.0.0"
  },
  "imports": {
    "#core/*": "./build/src/core/*",
    "#handler": "./build/src/handlers/index.js",
    "#handler/*": "./build/src/handlers/*",
    "#worker/*": "./build/src/workers/*",
    "#structures": "./build/src/structures/index.js",
    "#structures/*": "./build/src/structures/*",
    "#app": "./build/src/index.js",
    "#app/db": "./build/src/database/index.js",
    "#app/env": "./build/src/environment.js",
    "#worker/db": "./build/src/database/index.worker.js",
    "#native": "./build/src/n-api.js"
  },
  "napi": {
    "binaryName": "watklok"
  },
  "dependencies": {
    "discord-api-types": "^0.38.50",
    "http-proxy-agent": "^9.1.0",
    "seyfert": "^5.0.0",
    "socks-proxy-agent": "^10.1.0",
    "ws": "^8.21.1"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.7.3",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "start": "node ./build/src/index.js --expose-gc --optimize_for_size",
    "start:ts": "node ./src/index.ts --expose-gc --optimize_for_size",
    "start:bun": "bun ./src/index.ts --expose-gc --optimize_for_size",

    "build": "tsc",
    "build:native": "npm run build:copy && napi build --platform --release --cwd ./rs --esm --js index.mjs && npm run build:bindings",
    "build:native:dev": "npm run build:copy && napi build --platform --cwd ./rs --esm --js index.mjs && npm run build:bindings",
    "build:copy": "node -e \"const fs=require('fs');fs.mkdirSync('rs',{recursive:true});fs.copyFileSync('package.json','rs/package.json')\"",
    "build:bindings": "node -e \"const fs = require('fs'); const src = 'rs'; const dest = 'build/native'; if (!fs.existsSync(dest)) fs.mkdirSync(dest, {recursive:true}); const f = fs.readdirSync(src).find(x => x.endsWith('.node')); if (f) { fs.renameSync(src + '/' + f, dest + '/' + f); ['index.d.ts', 'index.mjs'].forEach(x => { const p = src + '/' + x; if(fs.existsSync(p)) fs.renameSync(p, dest + '/' + x) }); console.log('Moved ' + f + ' from ' + src + ' to ' + dest); } else { console.log('Source dir: ' + src); console.error('No .node file found in ' + src); process.exit(1); }\""
  }
}
