{
  "name": "untitles",
  "version": "0.1.1",
  "homepage": "https://github.com/SNIPPIK/UnTitles",
  "description": "Сделано с душой, не забывайте указывать авторство от этого зависит разработка!!!",
  "license": "BSD-3-Clause",
  "preferGlobal": true,
  "engineStrict": true,
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "discord.js": "^14.17.3",
    "dotenv": "^16.4.7",
    "sodium-native": "^4.3.1",
    "tiny-typed-emitter": "^2.1.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.4",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.2"
  },
  "scripts": {
    "start": "tsc -p tsconfig.json && cd build && node -r tsconfig-paths/register src --ShardManager",
    "shard": "tsc -p tsconfig.json && cd build && node -r tsconfig-paths/register src"
  }
}