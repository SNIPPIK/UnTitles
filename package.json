{
  "name": "untitles",
  "version": "0.0.1",
  "homepage": "https://github.com/SNIPPIK/UnTitles",
  "description": "This is probably the first music bot that doesn't copy others but presents something new and its own ",
  "license": "BSD-3-Clause",

  "preferGlobal": true,
  "engineStrict": true,

  "engines": { "node": ">=22.0.0" },

  "dependencies": {
    "tiny-typed-emitter": "^2.1.0",
    "sodium-native": "^4.3.1",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/websocket": "^1.0.10",
    "tsconfig-paths": "^4.2.0",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "scripts": {
    "start": "tsc -p tsconfig.json && cd build && node -r tsconfig-paths/register src --ShardManager",
    "shard": "tsc -p tsconfig.json && cd build && node -r tsconfig-paths/register src"
  }
}
