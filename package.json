{
  "name": "watklok",
  "version": "0.5.0",
  "homepage": "https://github.com/SNIPPIK/UnTitles",
  "bugs": "https://github.com/SNIPPIK/UnTitles/issues",
  "author": {
    "name": "SNIPPIK",
    "url": "https://github.com/SNIPPIK"
  },
  "license": "BSD-3-Clause + custom restriction",
  "preferGlobal": true,
  "engineStrict": true,
  "types": "src/index.ts",
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@discordjs/builders": "^1.14.1",
    "discord-api-types": "^0.38.47",
    "discord.js": "15.0.0-dev.1776730371-8ea7c7c7e",
    "http-proxy-agent": "^7.0.2",
    "socks-proxy-agent": "^8.0.5",
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^25.6.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "configure": "node -e \"require('fs').copyFileSync('.env', 'build/.env')\"",
    "start": "cd build && node -r tsconfig-paths/register src --ShardManager --optimize_for_size --expose-gc --experimental-require-module --no-compilation-cache --interval-stack-size",
    "build": "tsc",
    "build:native": "napi build --platform --release --cargo-cwd ./src-rs && node -e \"const fs = require('fs'); const p = 'build/native'; if (!fs.existsSync(p)) fs.mkdirSync(p, {recursive:true}); const f = fs.readdirSync('.').find(x => x.endsWith('.node')); if (f) { fs.renameSync(f, p + '/' + f); ['index.d.ts', 'index.js'].forEach(x => { if(fs.existsSync(x)) fs.renameSync(x, p + '/' + x) }); console.log('Moved ' + f + ' to ' + p); } else { console.log('Current dir: ' + process.cwd()); console.error('No .node file found here'); process.exit(1); }\"",
    "build:native:dev": "napi build --platform --cargo-cwd ./src-rs && node -e \"const fs = require('fs'); const p = 'build/native'; if (!fs.existsSync(p)) fs.mkdirSync(p, {recursive:true}); const f = fs.readdirSync('.').find(x => x.endsWith('.node')); if (f) { fs.renameSync(f, p + '/' + f); ['index.d.ts', 'index.js'].forEach(x => { if(fs.existsSync(x)) fs.renameSync(x, p + '/' + x) }); console.log('Moved ' + f + ' to ' + p); } else { console.log('Current dir: ' + process.cwd()); console.error('No .node file found here'); process.exit(1); }\""
  }
}
