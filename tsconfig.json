{
  "compilerOptions": {
    /* --- Основные настройки сборки --- */
    "target": "ES2022",                /* Собираем в современный JS (Node 18+ поддерживает ES2022 полностью) */
    "module": "NodeNext",              /* Важно для поддержки imports (#core/...) и Top-level await */
    "moduleResolution": "NodeNext",    /* Современный алгоритм разрешения модулей Node.js */
    "lib": ["ESNext"],                 /* Включаем новейшие фичи языка */

    "baseUrl": "./",
    "outDir": "./build",

    "sourceMap": false,

    /* --- Строгость (Type Checking) --- */
    "strict": true,
    "alwaysStrict": true,
    "strictNullChecks": false,
    "strictFunctionTypes": true,

    "noImplicitThis": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noImplicitAny": false,

    /* --- Совместимость и производительность --- */
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "removeComments": true,

    /* --- Декораторы --- */
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,

    "paths": {
      "#core/*": ["./src/core/*"],

      "#handler": ["./src/handlers/index"],
      "#handler/*": ["./src/handlers/*"],
      "#worker/*": ["./src/workers/*"],

      "#structures/*": ["./src/structures/*"],
      "#structures": ["./src/structures/index"],

      "#app": ["./src/index"],
      "#app/db": ["./src/database/index"],
      "#app/env": ["./src/environment"],

      "#worker/db": ["./src/database/index.worker"]
    }
  },

  "include": [
    "package.json",
    "tsconfig.json",
    "src"
  ],

  "exclude": [
    "node_modules"
  ]
}
